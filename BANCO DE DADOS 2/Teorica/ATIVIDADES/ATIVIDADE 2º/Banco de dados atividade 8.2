8.2
Retorna o número de valores não nulos na coluna da expressão.

R: COUNT(coluna)

A palavra-chave usada para retornar somente valores não duplicados ou combinações de valores não duplicados em uma consulta.

R: DISTINCT

Retorna o número de valores não nulos exclusivos na coluna da expressão.

R: COUNT(DISTINCT coluna)

1)CODE: 

select count(id) as musicas
from d_songs;

R: 6 Musicas

2)CODE:

select count(DISTINCT id) as Locais
from d_events;

R: 2 Locais

3)CODE:

select 
count(DISTINCT song_id) as song_id,
count(DISTINCT cd_number) as cd_number
from d_track_listings;

R: SONG_ID = 5
CD_NUMBER = 4

4)CODE: 

select count(DISTINCT email) as Emails
from d_clients;

R: 3 Emails

5)CODE: 

select count(auth_expense_amt) as autorizado
from d_partners;

R: 1 Autorizado

6)CODE: 

SELECT COUNT(shoe_color), COUNT(DISTINCT shoe_color)
FROM shoes;

R: 4 no primeiro count e 3 no segundo

7)CODE: 

select round(avg(nvl(auth_expense_amt, 100000)), 2) as Resutado
from d_partners;

R: 166666.67

8)
a)F
b)V
c)V
d)F

9)
a)F
b)F
c)V
d)f

